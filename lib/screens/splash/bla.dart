
import 'package:flutter/cupertino.dart';

class Bla extends Container{

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Balla"),
      ),
    );
  }
}